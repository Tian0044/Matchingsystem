name: telemedicine_app_main
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.10.0 <3.0.0'
  flutter: ">=2.0.0 <2.1.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^4.0.2
  realm: ^0.1.0+preview
  fluttertoast: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0

flutter:
  uses-material-design: true
  assets:
    - images/


